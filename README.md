# pkt-web-docs

The official documentation web UI for [pkt](https://github.com/thesixers/pkt) — a universal project manager and dependency tracker.

Built with Vite + React. Docs are written in Markdown and live in the `/docs` directory.